[credential]
    # important to first clear any previous helper
    helper =
    helper = "!f(){ command -v git-credential-manager.exe >/dev/null 2>&1 && exec git-credential-manager.exe \"$1\"; exec git-credential-libsecret \"$1\"; }; f"
