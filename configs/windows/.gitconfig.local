# important to first clear any previous helper
[credential]
    helper =
    helper = manager
[core]
    preloadindex = true
    fscache = true
[gc]
    auto = 256

# in wsl we use the windows installed git.exe,
# thus we need to mark wsl dirs as 'safe'
[safe]
    directory = //wsl.localhost/*
